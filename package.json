{
  "name": "PurelyBeingHuman",
  "version": "1.0.0",
  "private": true,
  "description": "PurelyBeingHuman static author website for GitHub Pages",
  "scripts": {
    "dev": "npx serve . -l 3000",
    "start": "npx serve . -l 3000",
    "build": "echo 'Static site ready for GitHub Pages'"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.2.5",
    "playwright": "^1.56.1",
    "serve": "^14.2.4"
  }
}
